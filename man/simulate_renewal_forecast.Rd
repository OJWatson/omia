% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast_uncertainty.R
\name{simulate_renewal_forecast}
\alias{simulate_renewal_forecast}
\title{Simulate renewal-based count forecasts with uncertainty propagation}
\usage{
simulate_renewal_forecast(
  incidence,
  serial_interval,
  rt = 1,
  horizon = NULL,
  n_sims = 1000L,
  seed = NULL
)
}
\arguments{
\item{incidence}{Numeric vector of historical incidence counts.}

\item{serial_interval}{Numeric serial interval probability vector where the
first element corresponds to a lag of one time step.}

\item{rt}{Reproduction-number input. Can be:
\itemize{
\item a scalar (constant across all simulations and forecast days),
\item a vector of length \code{horizon} (time-varying but shared across
simulations), or
\item a matrix with \code{n_sims} rows and \code{horizon} columns (fully stochastic
paths).
}}

\item{horizon}{Number of forecast days. If \code{NULL}, inferred from \code{rt} when
\code{rt} is a vector or matrix. Defaults to 7 when \code{rt} is scalar.}

\item{n_sims}{Number of simulation paths when \code{rt} is scalar or vector.}

\item{seed}{Optional random seed.}
}
\value{
A tibble with one row per simulation/day containing \code{sim}, \code{day},
\code{rt}, \code{infectiousness}, \code{expected_incidence}, and simulated \code{incidence}.
}
\description{
Generates stochastic forecast trajectories from a renewal equation while
propagating uncertainty in reproduction numbers (\code{rt}) and process noise.
Future incidence is simulated recursively, so uncertainty compounds over the
full forecast horizon.
}
